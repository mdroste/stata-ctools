/*
 * creghdfe_types.c
 *
 * Type definitions implementation (currently empty - types are header-only)
 * Part of the ctools Stata plugin suite
 */

#include "creghdfe_types.h"

/* This file is intentionally minimal - all types are defined in the header.
 * It exists to provide a compilation unit for the types module. */
