/* Retired label-save protocol; both entry points reject stale ado wrappers. */
#ifndef CDECODE_IMPL_H
#define CDECODE_IMPL_H
#include "stplugin.h"
ST_retcode cdecode_main(const char *args);
ST_retcode cdecode_scan_main(const char *args);
#endif
